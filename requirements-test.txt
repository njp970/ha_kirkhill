# Test stack for the kirkhill integration.
#
# pytest-homeassistant-custom-component pulls a matching Home Assistant core
# plus pytest / pytest-asyncio / a pinned aiohttp. aioresponses is added on top
# for the lightweight API-client tests in tests/test_api.py (HA pins aiohttp to
# a version aioresponses supports, so they coexist in one environment).
pytest-homeassistant-custom-component
aioresponses>=0.7.6
